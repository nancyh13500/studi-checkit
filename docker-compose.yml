version: "3.9"

services:
  web:
    build: .
    container_name: studi-checkit-web
    ports:
      - "8081:80"
    volumes:
      - .:/var/www/html
    # Si j’ajoute une base MySQL plus tard, je lierai le service ici :
    # depends_on:
    #   - db

  # Service MySQL prêt à être activé plus tard si besoin :
  # db:
  #   image: mysql:8.0
  #   container_name: studi-checkit-db
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: studi_checkit
  #     MYSQL_USER: studi
  #     MYSQL_PASSWORD: studi
  #   volumes:
  #     - db_data:/var/lib/mysql
  #     - ./bdd/studi_checkit.sql:/docker-entrypoint-initdb.d/studi_checkit.sql
  #   ports:
  #     - "3306:3306"

#volumes:
#  db_data: